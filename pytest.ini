# This file configures pytest for the project.

[pytest]
# Add the project root directory to the Python path.
# This allows tests in the 'tests/' directory to import modules
# from the root directory (e.g., `uniprot_utils.py`).
pythonpath = .
